import { ApiClient, type FoxApiResult, type RequestOptions } from "../apiClient";
export declare class ServerService {
    private readonly apiClient;
    constructor(apiClient: ApiClient);
    getServers(params: {
        token: string;
        keyword?: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getProvisioningServers(params: {
        token: string;
        keyword?: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getWebServerDomain(params: {
        token: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getServerInfoByIndex(params: {
        token: string;
        serverIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    updateServerInfoByIndex(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    applyConfig(params: {
        token: string;
        sector: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    modifySiteSector(params: {
        token: string;
        asisSector: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getServerDomains(params: {
        token: string;
        sector?: string;
        keyword?: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    addServerDomain(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    modifyServerDomain(params: {
        token: string;
        sector: string;
        domainName: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    removeServerDomain(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    removeProvisionServer(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
}
