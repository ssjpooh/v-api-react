import { ApiClient, type FoxApiResult, type RequestOptions } from "../apiClient";
export declare class ContractService {
    private readonly apiClient;
    constructor(apiClient: ApiClient);
    addContract(params: {
        token: string;
        body: RequestOptions["body"];
        siteIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    addContractLog(params: {
        token: string;
        contractNo: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getContractLog(params: {
        token: string;
        contractNo: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getAllContractLogs(params: {
        token: string;
        siteIndex: string;
        groupID: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getContractDataInfo(params: {
        token: string;
        contractNo: number;
        siteIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getContractLogs(params: {
        token: string;
        contractNo: number;
        siteIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    patchContract(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getCurrentContract(params: {
        token: string;
        siteIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    getContractList(params: {
        token: string;
        siteIndex: string;
        cancelId?: string;
    }): Promise<FoxApiResult>;
    patchContractLogByIndex(params: {
        token: string;
        index: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
    modifyContractState(params: {
        token: string;
        body: RequestOptions["body"];
        cancelId?: string;
    }): Promise<FoxApiResult>;
}
